import React, { Component } from 'react';

class CustomButton extends Component {
  render() {
    return (
      <div>
      <button className="customButton">Signin</button>
      </div>
    );
  }
}

export default CustomButton;
